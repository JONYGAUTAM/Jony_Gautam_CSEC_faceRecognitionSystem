# Jony_Gautam_CSEC_faceRecognitionSystem
This project is a real-time attendance system that uses face recognition technology to automate the process of marking attendance. It utilizes a webcam interface, built with Flask, allowing users to capture their face directly from the browser. Upon recognition, the system logs the user’s name, date, and timestamp into an Excel-compatible CSV file.
